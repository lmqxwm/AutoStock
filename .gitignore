# API keys — never commit these
keys.txt

# News-discovered tickers (auto-managed locally, not shared via git)
news_discovered.txt
# keys.py is safe to commit (it contains no secrets, only reads keys.txt)

# Price data cache (large binary files, regenerated on first run)
data/

# Alarm reports (personal output files)
alarms/

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/

# Test / scratch notebooks and outputs (not for repo)
test.ipynb
test_executed.ipynb
test/

# Jupyter checkpoints
.ipynb_checkpoints/
*/.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/
.env

# macOS
.DS_Store

# Logs
logs/
*.log
